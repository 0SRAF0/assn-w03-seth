{
  "name": "assn-w03-seth",
  "version": "1.0.0",
  "description": "Basic AWS Lambda function with DynamoDB",
  "main": "lambda/index.mjs",
  "type": "module",
  "scripts": {
    "local": "sam local start-api --port 3001",
    "local:function": "sam local invoke LambdaFunction",
    "build": "sam build",
    "deploy": "sam deploy --guided"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.431.0",
    "@aws-sdk/lib-dynamodb": "^3.431.0"
  },
  "author": "Seth McCarthy"
}
